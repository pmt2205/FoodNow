version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: host_app_cloud
    ports:
      - "80:80"
    environment:
      - DB_HOST=foodnow.cs9awkas4ybu.us-east-1.rds.amazonaws.com
      - DB_USER=admin
      - DB_PASSWORD=123456789a
      - DB_NAME=fooddb